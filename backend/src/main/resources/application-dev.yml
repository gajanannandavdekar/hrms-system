server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/hrms_db
    username: root
    password: root

  jpa:
    hibernate:
      ddl-auto: validate

    show-sql: true

  flyway:
    enabled: true

logging:
  level:
    org.springframework.security: DEBUG